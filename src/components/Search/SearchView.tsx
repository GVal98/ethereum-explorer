import Link from 'next/link'
import { Table, Button, Autocomplete } from '@mantine/core'
import { Address } from '../common/Address'

interface SearchViewProps {
  input: string
}

function SearchView(props: SearchViewProps) {
  return ( <></>
  )
}

export { SearchView }